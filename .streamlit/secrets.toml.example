# Copy this file to `secrets.toml` in the same folder and fill in your password.
# Never commit `secrets.toml` — it's already in .gitignore.
#
# On Streamlit Cloud, set the same value in: App → Settings → Secrets.

password = "change-me"
